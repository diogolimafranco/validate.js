{
  "name": "validate.js",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "minify": "uglifyjs dist/validate.js --compress --mangle --warn --output dist/validate.min.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d dist",
    "watch": "babel src -d dist --source-maps --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@babel/cli": "^7.1.5",
    "@babel/core": "^7.1.5",
    "@babel/preset-env": "^7.1.5",
    "uglify-js": "^3.4.9"
  }
}
